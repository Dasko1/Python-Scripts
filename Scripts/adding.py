def add(a, b):
    result = a + b
    return result

print("\nThe sum is: ",add(2,2))


"""
The flow goes like this: 1st) the flow goes to l.1, then to l.5, the function call. The function
call takes the arguments and passes them into the parameters in l.1. The logic happens in l.2,
and then the output keyword is "result"; this is the output of the entire function "add"!
""" 