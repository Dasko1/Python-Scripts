# To get any name in l.6, remove the parameter "name" in l.4 & remove name from the
# function call. "name" in l.4 is the parameter & the String in l.8 is the argument!

def get_user_name(name):
    # name = input("Enter your name: ")
    print("\nHello " + name + "!")

get_user_name("Hollie")